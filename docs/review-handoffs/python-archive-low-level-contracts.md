# Builder handoff: low-level Python oracle contracts

## Intent

- Replace three retained live-Python migration test dependencies with frozen,
  independently produced contracts.
- Move exhaustive Unicode lowering, lossless artifact JSON, and import staging
  into the default Python-disabled Node suite.
- Keep every production/runtime path, pack, artifact format, Terraform
  operation, and provider behavior unchanged.

## Base / Head

- Base: `7e1e65487a248c4484c9df75d3b0202e0a6c106a`
- Implementation head: `cc33757cbca0adc9b350b6363f888b6e716801cd`
- Diff command: `git diff 7e1e65487a248c4484c9df75d3b0202e0a6c106a..cc33757cbca0adc9b350b6363f888b6e716801cd`

## Files Changed

- `node-tests/python-lower-151.test.ts`
- `node-tests/python-lossless-artifact.test.ts`
- `node-tests/import-staging.test.ts`
- `node-tests/fixtures/python-lossless-binary64-v1.json`
- `node-tests/node-test-suite-selector.test.ts`
- `node-tests/pack-test-requirements.json`
- `docs/python-oracle-contracts.md`
- `docs/python-archive-plan.md`
- Files intentionally left untouched: all production TypeScript, retained
  Python, packs, schemas, generated runtime artifacts, Make targets, and the
  shared `node-tests/python-oracle.ts` resolver.

## Source Inputs Consulted

- Provider schemas: N/A; no provider projection or schema behavior changed.
- OpenAPI/API contracts: N/A.
- Provider source files: N/A.
- Pack metadata: existing committed pack/profile metadata was exercised by the
  unchanged import-staging profile matrix; it was not edited.
- Existing docs or design records: `docs/python-archive-plan.md`,
  `docs/python-oracle-contracts.md`, and the prior archive review handoff.
- Other source evidence: the live tests at the base commit, executed with
  CPython 3.13.13 / UCD 15.1.0 before their imports were removed. The exact
  resurrection worktree and test command are recorded in
  `docs/python-oracle-contracts.md`.

## Generated Artifacts

- Reports: this handoff only.
- Schemas: none.
- Fixtures: `node-tests/fixtures/python-lossless-binary64-v1.json`, containing
  the producing baseline, Python/UCD authority, corpus counts, exact output
  byte length, and SHA-256.
- Snapshots: none.
- Demo or lab outputs: none.
- Artifact drift intentionally expected: none outside the new frozen test
  fixture. No operational artifact or committed golden changed.

## Expected Delta

- Expected behavior change: the three converted test files run without Python
  and are selected by the default Node suite.
- Expected report/count/coverage changes: default selection increases from 48
  to 51 files; live Python-oracle exclusions decrease from 20 to 17. The
  default selected suite increases from 479 to 506 tests.
- Expected generated-output changes: none.
- Expected no-op areas: production/runtime code, Terraform behavior, packs,
  provider behavior, artifact bytes, release bundle, and the remaining live
  Python differential suite.

## Invariants Claimed

- Evidence must not be silently dropped: Unicode coverage remains exhaustive
  over every scalar and five context vectors; lossless JSON checks exact bytes
  or exact-byte digests; import staging retains all eight parser cases and all
  canonical/CR/CRLF/BOM cases.
- Generic matcher evidence must not outrank source-backed evidence: N/A; no
  matching or source-evidence behavior changed.
- Source precedence/provenance must remain explicit: both the documentation and
  binary64 fixture name the baseline and CPython/UCD authority. The fixture is
  not generated by the Node implementation under test.
- Ambiguity must stay classified instead of being coerced to success: malformed
  and unterminated import blocks still fail closed; invalid JSON containers and
  numeric lexemes still retain their existing failure assertions.
- Provider-readiness counts must stay explainable: N/A; no readiness surface
  changed. Suite selection counts are exact and regression-locked.
- Adoption safety invariants: state-aware staging still uses injected state,
  preserves source bytes, never contacts a backend in the frozen cases, and
  retains the existing missing/malformed/state-failure tests.

## Tests Run

- `npm run typecheck`: passed.
- Python-disabled focused four-file suite (three converted files plus selector):
  34 passed.
- `PYTHON=/definitely/missing/python npm test`: 506 passed; 51 selected and 17
  live-oracle files excluded.
- `npm run test:all`: 663 total, 662 passed, one platform skip, zero failed.
- `PYTHON=/definitely/missing/python node scripts/run-node-test-suite.mjs run -- --test-concurrency=1`:
  506 passed.
- `PYTHON=/definitely/missing/python node --test .node-test/node-tests/terraform-command.test.js`:
  23 passed.
- Python-disabled non-test `check-all` remainder (`check-pack-set`, examples,
  modules, tfvars format, packs, vendor boundary, root catalog): passed.
- `git diff --check`: passed.
- The default concurrent `make check-all` was attempted twice. Both attempts
  reached 504 passing selected tests before the unchanged 500 ms
  `terraform-command.test.ts` descendant-PID case read `descendant.pid` before
  the child created it. That same file passed 23/23 isolated and the complete
  selected suite passed 506/506 serially. This diff does not touch that test or
  its implementation.
- Tests not run: live credentials, provider calls, remote backends, or
  deployment Apply; no affected behavior requires them.

## Known Deferrals

- Seventeen test files still import the live Python oracle. They are unchanged
  and remain excluded from the Python-free default suite.
- The shared Python oracle resolver and its own test remain until the last live
  consumer is frozen.
- The pre-existing concurrent descendant-PID timing race is not changed in this
  archive slice; it is independently reproducible under aggregate load and
  passes isolated/serially.
- Follow-up trigger: the next bounded archive slice should freeze one coherent
  higher-level corpus without weakening byte contracts or deleting the shared
  harness prematurely.

## Review Focus

- Highest-risk files: all three converted tests and the binary64 fixture.
- Attack whether any live comparison became Node-versus-Node self-agreement.
- Reproduce the frozen values from the base worktree and recorded CPython/UCD
  authority, especially the binary64 byte count/digest and BOM behavior.
- Verify that the Unicode digest still covers the entire scalar domain and all
  original framing/context vectors rather than a sample.
- Verify that compacting the numeric-only binary64 output has exactly the same
  scope as the retired test and cannot hide number-token differences.
- Compare all eight import-filter expected objects and canonical/CR/CRLF/BOM
  staged bytes with the original Python results.
- Verify pack-requirement ordering and the 51/17 full/reduced-profile selector
  behavior.
- Reject any fixture or assertion that can be regenerated solely by the Node
  implementation under test without consulting the recorded authority.
