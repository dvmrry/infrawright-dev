{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://infrawright.local/schemas/zcc-adoption-artifact-materialization.schema.json",
  "title": "Infrawright ZCC Provider-Observed Adoption Artifact Materialization",
  "$comment": "A content-free receipt for exact bootstrap publication under a fresh provider-observed candidate and complete ready adoption parity assertion. Complete proves exact bytes only; it does not claim plan, apply, live-tenant, or cutover readiness. Custom semantics are required to enforce the verification join and complete created/reused partition.",
  "type": "object",
  "x-infrawright-zcc-adoption-materialization-semantics": true,
  "additionalProperties": false,
  "required": [
    "kind",
    "schema_version",
    "mode",
    "product",
    "resource_type",
    "tenant",
    "status",
    "publication",
    "verification"
  ],
  "properties": {
    "kind": {
      "const": "infrawright.zcc_adoption_artifact_materialization"
    },
    "schema_version": {
      "const": 1
    },
    "mode": {
      "const": "bootstrap"
    },
    "product": {
      "const": "zcc"
    },
    "resource_type": {
      "$ref": "zcc-pull-artifact-set.schema.json#/$defs/resourceType"
    },
    "tenant": {
      "type": "string",
      "pattern": "^(?!\\.{1,2}$)[A-Za-z0-9_.-]+$"
    },
    "status": {
      "const": "complete"
    },
    "publication": {
      "$ref": "#/$defs/publication"
    },
    "verification": {
      "allOf": [
        {
          "$ref": "zcc-adoption-artifact-parity.schema.json"
        },
        {
          "type": "object",
          "properties": {
            "status": {
              "const": "ready"
            },
            "parity": {
              "type": "object",
              "properties": {
                "status": {
                  "const": "equal"
                }
              }
            }
          }
        }
      ]
    }
  },
  "$defs": {
    "artifactName": {
      "enum": [
        "imports",
        "lookup",
        "tfvars"
      ]
    },
    "artifactNames": {
      "type": "array",
      "maxItems": 3,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/artifactName"
      }
    },
    "publication": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "policy",
        "created",
        "reused"
      ],
      "properties": {
        "policy": {
          "const": "create_or_verify_exact"
        },
        "created": {
          "$ref": "#/$defs/artifactNames"
        },
        "reused": {
          "$ref": "#/$defs/artifactNames"
        }
      }
    }
  }
}
