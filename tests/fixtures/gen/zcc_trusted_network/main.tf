# GENERATED by engine.gen_module from packs/zcc/schemas/provider/zcc.json — do not edit.
# Regenerate: python -m engine.gen_module

resource "zcc_trusted_network" "this" {
  for_each = var.items

  active                    = each.value.active
  condition_type            = each.value.condition_type
  dns_search_domains        = each.value.dns_search_domains
  dns_server_ips            = each.value.dns_server_ips
  hostname                  = each.value.hostname
  name                      = each.value.name
  resolved_ips_for_hostname = each.value.resolved_ips_for_hostname
  ssid                      = each.value.ssid
  trusted_dhcp_servers_ips  = each.value.trusted_dhcp_servers_ips
  trusted_egress_ips        = each.value.trusted_egress_ips
  trusted_gateway_ips       = each.value.trusted_gateway_ips
  trusted_subnet_ips        = each.value.trusted_subnet_ips
}
