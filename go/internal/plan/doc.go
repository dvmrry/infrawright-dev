// Package plan ports the deterministic saved-plan fingerprint contract from
// node-src/domain/plan-fingerprint.ts. Fingerprints and digests produced by
// this package are artifact bytes and therefore preserve the Node oracle
// exactly.
package plan
